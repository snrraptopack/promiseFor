# promiseFor

A simple promise wrapper that accepts both callback-based and promise-based functions and returns a tuple [result, error].

## Installation

You can install the package using npm:

```bash
npm install promiseFor
